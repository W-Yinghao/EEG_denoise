# Population posteriors

Reserved for the auditable clean-prior plus population-observation composition. Implementation is
blocked by `CONFLICT-SCI-001`; an unconditional prior must never be substituted for `NULL`.
