# Provenance

Reserved for immutable configuration, data, split, checkpoint, environment, code, allocation, and
artifact hashes shared through schema-bound files across the two registered environments.
