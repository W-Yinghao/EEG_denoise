# Data boundary

Reserved for registry-backed readers and immutable support/query split types. It may depend only on
registered data, schemas, and provenance; no reader or scientific implementation exists yet.
