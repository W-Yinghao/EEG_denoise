# Energies

Reserved for separate `E0`, `EC`, and `EC-E0` components after population-base semantics are
resolved. This layer may depend on posteriors and support calibration, never on evaluation results.
