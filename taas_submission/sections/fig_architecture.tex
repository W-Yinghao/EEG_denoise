\begin{figure*}[t]
  \centering
  \includegraphics[width=0.96\textwidth]{figures/method_protocol.pdf}
  \caption{Same-session subject-calibrated, EOG-guided protocol. Early support
  EEG+EOG estimates $\mathbf H_s$. Later query EEG and EOG produce the linear
  anchor $\mathbf a_0=\mathbf H_s\mathbf e$; deterministic and diffusion
  residuals complete the correction. Clean targets, labels, and natural-query
  evaluator annotations are isolated from inference.}
  \label{fig:architecture}
  \Description{An early EEG and EOG support block estimates a subject operator.
  A later EEG and EOG query passes through a linear anchor, deterministic
  residual estimator, diffusion residual estimator, and artifact subtraction.
  Evaluator-only fields connect only to the evaluation block.}
\end{figure*}
