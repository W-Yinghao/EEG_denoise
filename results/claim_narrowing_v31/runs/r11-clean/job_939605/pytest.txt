..........................F..                                            [100%]
=================================== FAILURES ===================================
________________ test_recovery_lineage_is_not_silently_accepted ________________

    def test_recovery_lineage_is_not_silently_accepted():
        rows = run_v31._lineage()
        by_job = {row["job_id"]: row for row in rows}
        if "939597" in by_job:
            assert by_job["939597"]["status"] == "failed"
            assert by_job["939598"]["status"] == "recovery"
            assert by_job["939598"]["recovery_of"] == "939597"
            assert by_job["939598"]["scientific_setting_changed"] is False
        else:
            # Clean git archives intentionally exclude runtime result directories.
            source = inspect.getsource(run_v31._lineage)
>           assert 'job_id == "939597"' in source
E           assert 'job_id == "939597"' in 'def _lineage() -> list[dict[str, Any]]:\n    rows: list[dict[str, Any]] = []\n    for job in sorted((RESULT / "runs")...  "recovery_of": recovery_of,\n                "scientific_setting_changed": False,\n            })\n    return rows\n'

tests/unit/test_claim_narrowing_v31.py:227: AssertionError
=========================== short test summary info ============================
FAILED tests/unit/test_claim_narrowing_v31.py::test_recovery_lineage_is_not_silently_accepted
1 failed, 28 passed in 3.42s
