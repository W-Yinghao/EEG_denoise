# FIGURES_CATALOG_V3 — 图库材料目录（供作者挑选，2026-08-31）
全部 13 张：PDF+PNG 在 `artifacts/figures/v3/`，脚本在 `scripts/paper_final/figures/`（共享样式 `figstyle.py`，一臂一色全局统一）。规则已执行：无 CI/误差棒（逐被试点/线代替；波形上的 80% 预测带是研究对象、予以保留）、图内极简文字、全部数据运行时从盘上文件读取。
每张图的渲染说明（构建 agent 的如实记录，含数据意外与取舍）如下——**选用与否、进正文还是附录，由作者决定**。

## fig-overview — Method overview (three-stage pipeline)  [main]
- **文件**: `artifacts/figures/v3/fig-overview.pdf` ｜ **脚本**: `scripts/paper_final/figures/fig_overview.py`
- **支撑论断**: The system calibrates a propagation matrix from a separate 120-s segment, guides a shared diffusion model with propagated EOG, withholds the guide when calibration fails the reliability criterion, and reports intervals that include propagation uncertainty.
- **诚实性要点**: Diagram only — no measured values may appear; the fallback (refusal) path must be drawn as a first-class branch, not omitted.
- **经过修复轮**（原违规：1) Overlapping labels: "EB shrink (Eq. 3)" (fig_overview.py:98, fs 5.5 centered at x=0.173) overflows its container box (x 0.132-0.214) — the leading "E" sits on the left border and the closing ")" crosses the right border (confirmed at 4x zoom). 2) Overlapping labels: the Eq. 4 text "$\hat{x}_0 = (…）
- **渲染记录/取舍**: Fixed both counted violations plus the two marginal ones, verified by measuring rendered text extents in figure coordinates (renderer-based bboxes) and by 4x-zoom inspection of the PNG. (1) "EB shrink (Eq. 3)" reflowed to three lines inside the ridge box — "ridge (Eq. 2)" / "EB shrink" / "(Eq. 3)" at fs 5.3; widest line now spans x[0.140,0.206] inside the box interior [0.132,0.214]. (2) Eq. 4 line tightened with mathtext {=}/{-} spacing, fs 6.2->5.8, left-anchored moved 0.622->0.612; right edge now 0.6756, clear of the B/C separator chevron (left edge 0.6815) and dotted line at 0.685. (3) "reliable?" fs 5.2->4.8 and diamond half-width 0.026->0.028: text [0.2276,0.2684] inside diamond [0.220,0.276]. (4) "$y$" label: the first retry (above the snippet) visually collided with the dashed fallback arrow, so instead the contaminated-y snippet axes moved right 0.343->0.352 and the label sits centered in the widened gap at x[0.3360,0.3460] — >=0.005 from both the separator line (0.330) and the axes edge (0.352); the U-Net input arrow start moved 0.411->0.420 to match. No panels dropped; no content, data, or color changes — all arrays still loaded at runtime from paper_final_arrays/t6_waveform_exemplar_dev.npz (Fp1), only the allowed 80% predictive band is shaded, no performance numbers. Script exits 0 run from the repo root with the icml python.

## fig-denoise — Paired denoising and participant effects  [main]
- **文件**: `artifacts/figures/v3/fig-denoise.pdf` ｜ **脚本**: `scripts/paper_final/figures/fig_denoise.py`
- **支撑论断**: Calibrated guidance improves paired restoration for every dev participant (matched 0.4310 vs unguided 0.5738, 15/15 positive) and the improvement has ocular topography (frontal maximum).
- **诚实性要点**: The linear_regression trace stays in Panel A so the near-parity of diffusion and calibrated linear regression as point estimators (0.4310 vs 0.4353) is visible, not hidden. The held-out per-participant paired panel of the original placeholder cannot be built from banked arrays (see gaps); the held-out aggregate (0.5351 vs 0.6888, 7/8 positive) is caption/table text.
- **渲染记录/取舍**: No panels dropped. Panel A: 3-column small multiples (Fp1/Fp2/AFz) x 5 arm rows (contaminated, reference, linear, unguided, matched+80% band) with a spanning EOG-drive strip on top; reference overlaid thin gray under each reconstruction so the linear-vs-matched near-parity is visible; time base 100 Hz (configs/calib_saddpm_cond_v42r/data.yaml), 512 samples = 5.12 s; scale bars only, axes off. Data surprises: (1) sub-09 is an extreme outlier (mean rRMSE 3.43 -> 3.40, vs cohort ~0.1-0.4), so Panel B uses a log y-axis to keep all 15 paired slope lines readable while still showing sub-09's near-flat pair honestly; (2) the spec's pooled numbers 0.5738 -> 0.4310 reproduce exactly ONLY when the 100/720 zero_artifact rows are included in the per-participant means — the figure aggregates that way and annotates the runtime-computed 0.574/0.431 and 15/15 (down-arrow); (3) the exemplar's recorded EOG drive is HEOG-dominated (VEOG nearly flat) — plotted as-is. Panel C: 32-channel mne standard_1020 topomap (Oranges, vlim 0..0.29, Fp1/Fp2/AFz ring-marked linking to Panel A, single "Fp1 +0.29" annotation from data); the 14 ear channels have no montage positions so they are a color-coded dot strip beside the head with 4.5pt names — per the mandatory fix there is no "near zero" (or any) text claim about them; their small values show through the pale colors. No CIs/error bars anywhere; the only band is the 80% predictive interval on the matched trace (subject of the paper). Script exits 0; PDF+PNG written by figstyle.save to artifacts/figures/v3/.

## fig-natural — Natural behavior and owner specificity  [main]
- **文件**: `artifacts/figures/v3/fig-natural.pdf` ｜ **脚本**: `scripts/paper_final/figures/fig_natural.py`
- **支撑论断**: On natural recordings the matched guide removes more EOG while preserving more low-EOG signal than any control or anchor operating point; wrong-owner and shuffled guides actively degrade (negative attenuation, collapsed retention) — the benefit is owner-specific.
- **诚实性要点**: ICA/ASR/eye-subspace attenuate MORE dB than matched (up to 4.87 vs 2.46) — the figure must not suggest raw attenuation superiority; the claim is the joint operating point above the retention bar. The placeholder's RLS adaptation-curve panel has no banked array and moves to gaps.
- **渲染记录/取舍**: No panels dropped. Data surprises and choices: (1) No per-participant arrays exist in the three named files — the contrasts block carries only mean/median/bootstrap bounds/positive_count summaries — so the mandated whisker replacement for Panel B is a 5-contrast x 3-metric dot plot (filled dot = mean, open dot = median, thin stem to zero) with the per-participant sign information shown as tiny gray k/15 counts; all bootstrap bounds and the npz attenuation_ci/retention_ci arrays are deliberately unused (rule 2), including in Panel A. (2) The JSON has 5 contrasts, not the 3 the spec listed — all 5 are plotted (adds minus_NO_A0 and minus_WRONG), which honestly shows the one negative result: MATCH_gated retention is below NO_A0 for 0/15 participants (delta -0.078) — gating trades a little retention for +2.19 dB attenuation. (3) Panel C was densified with the three CPU anchors as open bars alongside the six condition bars; honesty preserved — ICA (0.219) and SGEYESUB (0.213) reduce coherence more than MATCH_gated (0.190), and in Panel A SGEYESUB attenuates most (4.87 dB) but sits far left of the 0.75 retention validity bar; MATCH_gated (0.843 retention, 2.46 dB) is the only marker in the lightly shaded healthy region (retention >= 0.75, attenuation > 0). The 0.75 bar is annotated with its source number as the one allowed reference tick. All plotted numbers are read from the three named data files at runtime; figstyle.C colors used for every arm; script exits 0.

## fig-loop — Calibration economics and operator staleness  [main]
- **文件**: `artifacts/figures/v3/fig-loop.pdf` ｜ **脚本**: `scripts/paper_final/figures/fig_loop.py`
- **支撑论断**: 60 s of calibration buys ~97% of the 120-s gain; the 30-s floor is a designed refusal (gate fires 100%), not a failure; the calibrated operator has a measurable lifetime (displacement 86% of norm at 2 min, 143% at 8 min) while the paired gain by record third stays flat-to-rising — what expires is the operator, not the mechanism.
- **诚实性要点**: Both readings shown at 30 s: the system value sits BELOW the unguided floor (-0.016) and the rule-off value (0.124) is also plotted, so the figure does not hide that raw 30-s calibration would still carry most of the gain.
- **渲染记录/取舍**: No panels dropped; all three rendered from the named files. verdict.fix was empty — nothing to apply. Deviations from the spec text, all forced by the frozen house rules: (1) every bootstrap band/CI mentioned in the spec was omitted (rule 2); since no per-participant arrays exist in these files, the replacement density comes from the banked JSON medians plotted as a second dashed open-marker series beside each mean (displacement, natural attenuation, gain-by-third), plus the JSON positive counts ("14/15 subj. > 0" in Panel C, runtime-read). (2) Panel B's requested twin y-axis became two stacked axes sharing one elapsed-time x-axis (dual y-scales are a chart anti-pattern); the drift-rises-as-natural-benefit-falls contrast survives intact. Data surprises: npz means match JSON means exactly (asserted in-script); displacement medians (0.47-0.63) sit well below means (0.86-1.43), so the 86%/143% headline numbers are mean-driven — worth a caption word. Honesty note satisfied: 30-s system point (-0.016, below the unguided floor) and the 30-s rule-off point (0.124) are both plotted, with gate fractions annotated from data (100% at 30 s, 4.4-6.7% elsewhere) and the runtime-computed "97% of 120 s" tick at 60 s. Colors: system/matched arm = figstyle C["MATCH"] (rule-off = same arm, dashed), unguided floor = C["NO_A0"], natural attenuation = neutral gray (property of the raw record, not an arm). Figure is 7.0 x 1.95 in (~49.5 mm tall vs the 46 mm placeholder; scales to width with near-identical aspect).

## fig-uq — Predictive intervals and calibration  [main]
- **文件**: `artifacts/figures/v3/fig-uq.pdf` ｜ **脚本**: `scripts/paper_final/figures/fig_uq.py`
- **支撑论断**: Propagation-plus-temperature intervals are calibrated at no sharpness cost (held-out 0.810/0.864 at nominal 0.80/0.90 under dev-frozen scalars, CRPS 0.3800 vs 0.3824 temperature-only), and interval width carries a real operator-propagation component.
- **诚实性要点**: One held-out participant shows clear 80% under-coverage (0.456) — plotted, not suppressed. The diffusion-vs-deterministic-ensemble comparison (CRPS 0.1529 vs 0.1548; conformal 0.765 vs 0.652) and the dev 50% coverage level have no banked arrays and remain table/text (see gaps).
- **渲染记录/取舍**: All four panels kept. Data surprises and choices: (1) The spec's "mean +/- 1.2816*sigma" band would NOT reproduce the banked coverage — numerically verified that the banked per-policy per-participant coverages are reproduced exactly by band = T * sqrt(sigma^2 + var_op) with the banked temperature (prop+temp T=2.4 gives 0.4564/0.7738; temperature_only = 3.0*sigma; raw = 1.0*sigma). Panel C therefore draws the propagation_plus_temperature policy band with T read from the npz, and the script asserts (tol 2e-3) that this reconstruction matches the banked per-participant coverage before plotting. (2) Mandatory fix applied: sub-01 and sub-04 are shown as two held-out participants labeled only with their own 80% coverages (0.46 and 0.77, both below nominal); no best/worst wording anywhere (sub-10, the true best at 0.943, has no banked exemplar array). (3) Exemplar picks: sub-01 ep3/ch13 (channel coverage 0.45, matches the subject aggregate 0.456); sub-04 ep4/ch32 (0.87, chosen for band visibility — sub-04's mid-coverage channels sit on episodes with huge reference drift, e.g. ep5 ref std 155, which hides the band at trace scale). Zero-artifact episodes (ep7 in both files) are appended as 256-sample negative controls after a divider: var_op collapses to ~0 there, visible as the white operator-share strip, and the band collapses too. (4) Operator-propagation share var_op/(sigma^2+var_op) is drawn as a sequential single-hue strip (0->1) under each waveform, non-degenerate on contaminated segments (mean ~0.3-0.4, peaks ~0.7). (5) Panel D shows the dev prop+temp CRPS 0.150 as a labeled dashed reference tick on the same CRPS axis rather than a second axis (no dual axes; same unit, different corpus — the caption should state dev is a different corpus). Dev 50% coverage not plotted (not banked), per spec. Panel A: temp-only and prop+temp calibration curves coincide (0.813/0.810 at 0.80) — drawn solid orange under dashed blue so the overlap is legible; dev-frozen reference points at 0.80/0.90 as open diamonds. Colors follow the fig-sealed55 policy convention (raw=gray RAW, temperature-only=sky POP, prop+temp=vermillion MATCH).

## fig-sealed55 — Sealed 55-participant confirmation  [main]
- **文件**: `artifacts/figures/v3/fig-sealed55.pdf` ｜ **脚本**: `scripts/paper_final/figures/fig_sealed55.py`
- **支撑论断**: On a pre-registered sealed cohort of 55 subjects the matched-calibration gain confirms (53/55 positive vs zero at n=259, 55/55 vs wrong-owner, verdict CONFIRMED), is flat from 30 to 259 training participants, and dev-frozen temperatures transfer (0.830/0.891 coverage at nominal 0.80/0.90).
- **诚实性要点**: Gate B3 is FALSE: the CRPS ordering did not reproduce (propagation 0.1375 vs temperature-only 0.1363) — must be stated in the caption if CRPS is mentioned, and the UQ verdict is UQ_CONFIRMED_SECOND_CORPUS (claim restricted to the ambulatory corpus). Per-subject 80% coverage spans 0.22-1.00 and is disclosed in full. It is 53/55, not 55/55, for gain-vs-zero at n=259.
- **渲染记录/取舍**: All four panels kept; none dropped. Panel B redesigned per the mandatory fix: the spec's CI-whisker panel became six per-subject dot columns (gain zero-own at n=30/n=259, independent probe cohort as 8 hollow dots, own-wrong at n=30/n=259, per-subject trend n259-n30) with mean ticks and positive counts (54/55, 53/55, 8/8, 55/55, 55/55) read from sealed_confirm_decision.json / probe.json; trend column labeled "flat" with its mean -0.006 annotated. Data surprises: (1) probe.json rows contain 8 BLIND rows alongside the 8 COND rows — filtered on presence of rrmse_zero; (2) rRMSE max is 2.77 (one subject's wrong-owner value), so Panel A uses a log y-axis to 2.0+; (3) trend is 21/55 positive per-subject (mean -0.006, consistent with flat) — the count is not annotated, only "flat" per spec; (4) trend statistic lives under key trend_nmax_minus_nmin as the verdict noted. Panel D uses the verified query injection ratio (span 0.566-6.345), log-x, both n=30 (hollow) and n=259 (filled) gains. Panel C shows all 55 per-subject 80% coverages for the three UQ policies (raw pooled 0.407, temp-only T=3.25 pooled 0.834, prop+temp T=2.45 pooled 0.830) with full 0.084-0.998 spread visible and the nominal 0.80 line. CRPS is not depicted, so the B3 caption caveat only applies if the caption mentions CRPS; the 53/55 (not 55/55) gain-vs-zero count at n=259 is printed directly on the panel. Medians in Panel A (0.331/0.373/0.477, blind 0.347) are computed at runtime from sealed_rows.json and match the spec. Script exits 0; PDF+PNG confirmed on disk.

## fig-downstream — Downstream utility in three currencies  [main]
- **文件**: `artifacts/figures/v3/fig-downstream.pdf` ｜ **脚本**: `scripts/paper_final/figures/fig_downstream.py`
- **支撑论断**: Calibrated restoration is healthy in all three currencies — decoding accuracy (no harm), ERP AUC (no harm, unguided alone harms), and ERP morphology preservation (matched 0.931/0.966) — while classical arms diverge: ASR wins held-out AUC but collapses preservation to 0.637, and ICA/eye-subspace significantly harm a capacity decoder.
- **诚实性要点**: The pre-registered SSVEP gain expectation did NOT materialize (matched -0.001 overall, -0.007 in the highest-EOG tertile) — the claim is strictly no-harm, not improvement, and the null must be visually obvious. ASR's genuine held-out AUC advantage is plotted, not suppressed; the preservation collapse is shown beside it rather than used to erase it.
- **渲染记录/取舍**: Data surprise: dwave_dev.npz and dwave_heldout.npz bank ONLY per-arm summaries for D1/D2 (mean, median, positive_count/participants, bootstrap bounds) — no per-participant rows exist for those endpoints anywhere in paper_final_arrays, so the mandatory-fix per-participant dots are impossible for Panels A/B. Instead A/B show mean (filled dot) + median (open dot) per arm with 'k/n participants improved' counts — the only per-participant information those files contain — and the banned bootstrap bounds were not plotted. Panel C uses true per-participant paired deltas from the deep-ERP rows (dev n=15, ASR n=14 because one participant lacks an ASR row; held-out n=8, ASR n=7), jittered dots + arm-mean tick, verified against banked contrast means by assert. Panel A includes the MATCH-by-EOG-tertile L/M/H ticks in both cohorts (H negative: -0.0072 dev / -0.0082 held-out, making the pre-registered-gain failure visible). Panel D plots preservation vs D2 AUC delta for both cohorts (open=dev, filled=held-out, paired by arm-colored lines): held-out ASR (0.637, +0.042) vs MATCH (0.966, -0.002). No panels dropped. The mandatory fix's '2a n=9' refers to bci2a arrays that are not among this figure's named data files, so no 2a panel was added. RAW absolute values appear as small gray header annotations (allowed reference numbers): D1 0.773/0.832, D2 0.750/0.768, EEGNet 0.769/0.752.

## fig-ablation — Guide-feature ablation and shrinkage  [appendix]
- **文件**: `artifacts/figures/v3/fig-ablation.pdf` ｜ **脚本**: `scripts/paper_final/figures/fig_ablation.py`
- **支撑论断**: The guide carries the payload (removing it costs +0.142-0.158), calibration features are auxiliary (+0.005 when swapped), and empirical-Bayes shrinkage is what makes a 2-minute calibration deployable (unshrunk lambda=1 costs +0.347).
- **诚实性要点**: Cells are single-seed (seed 20261201) with shared episodes/noise — say so in the caption; the wide unshrunk CI (+0.004 to +1.025 in the manuscript table) means 'occasionally catastrophic', not 'always'.
- **渲染记录/取舍**: No panels dropped. Data as expected: cells/contrasts are JSON in 0-d unicode arrays, parsed per verdict.fix; all 6 cells and all 5 contrasts match spec values. One addition beyond spec: Panel B shows all 5 contrasts from the file (spec named only 3) for density — the extra two (none|population +0.158, population|population +0.186) also back the claim's +0.142-0.158 range. Sparse grid handled: population|matched and matched_unshrunk|population drawn as hatched not-run cells. Per house rule 2, the unshrunk arm's wide range (+0.004 to +1.025) is NOT drawn — the caption must quote it plus single-seed (seed 20261201, shared episodes/noise) provenance from the manuscript table, as neither is in the npz. matched_unshrunk uses the matched arm color as an open/hatched bar (it is a variant of the matched arm, not a new arm in figstyle.C).

## fig-deepnull-bci — Deep-decoder and BCI IV-2a null results  [appendix]
- **文件**: `artifacts/figures/v3/fig-deepnull-bci.pdf` ｜ **脚本**: `scripts/paper_final/figures/fig_deepnull_bci.py`
- **支撑论断**: Denoising does not move motor-imagery decoding on BCI IV-2a by either route, and deep SSVEP decoding is null — honest no-benefit results, with mechanical evidence (anchored 0.142 vs unguided 0.683) that the guide carries signal even where accuracy is unchanged.
- **诚实性要点**: These are the paper's disclosed nulls: every 2a CI straddles zero and the deep SSVEP endpoint is underpowered (EEGNet 0.35 vs CCA 0.77 on the same task) — the figure exists to disclose them, not to rescue them.
- **经过修复轮**（原违规：Data-sanity mismatch in Panel A: titles claim "dev · n=15" and "held-out · n=8", but the ASR arm draws only 14 dots (dev) and 7 dots (held-out) — verified in the banked npz (dwave_deep_ssvep_dev.npz has 14 ASR rows, dwave_deep_ssvep_heldout.npz has 7; all other arms have 15/8). The ASR mean tick is …）
- **渲染记录/取舍**: Fixed the single inspector violation: Panel A's ASR arm has only 14 dev / 7 held-out rows in the banked npz (one participant missing per cohort, consistent with the known asrpy per-cell calibration failure) while the sub-axis titles state the full cohort n=15/n=8. The script now discloses this in-figure: any arm whose row count differs from the banked participants number gets a small "n=<count>" tag in the arm's color just right of its dots (renders as n=14 dev, n=7 held-out on the ASR row only), computed at runtime, placed clear of the chance label and CCA reference lines. No other changes; all four panels kept; no CIs/error bars/whiskers anywhere; all values still loaded at runtime from the five spec files plus dwave_dev.npz/dwave_heldout.npz (CCA references 0.77/0.83) and route2_gate.json (gate_pass, means 0.683 to 0.142) per verdict.fix; asserts against every banked mean pass. Verified the rendered PNG visually: annotations legible, no overlaps.

## fig-eegdfus-split — EEGDfus released-vs-strict split effect  [appendix]
- **文件**: `artifacts/figures/v3/fig-eegdfus-split.pdf` ｜ **脚本**: `scripts/paper_final/figures/fig_eegdfus_split.py`
- **支撑论断**: The released SSED split leaks (test indices drawn from the full range overlap training): EEGDfus drops from CC 0.980 to 0.889 and RRMSE_t 0.085 to 0.373 when the split is repaired, while our arms barely move — a reproducibility finding reported factually.
- **诚实性要点**: Our own SSED transplant is an honest negative — CC 0.48-0.49 sits BELOW the identity baseline 0.70, and fine-tuning hurts vs zero-shot (0.619 -> 0.484); both must be visible in the same panels. EEGDfus is credited where it earns it: it repairs delta-band PLV (0.68 -> 0.87) under the strict split while our transplant degrades it.
- **经过修复轮**（原违规：Panel C annotation "αβγ≥0.99 all arms (omitted)" is contradicted by the banked data: eegdfus/strict_test gamma PLV = 0.9894 (< 0.99) in results/paper_final/e5/plv.json; the claim only holds after rounding to two decimals. Fix the annotation (e.g. "≥ 0.98" or "≈ 0.99") at line 188 of scripts/paper_fi…）
- **渲染记录/取舍**: Single inspector violation fixed: Panel C annotation changed from "αβγ≥0.99 all arms (omitted)" to "αβγ≥0.98 all arms (omitted)" (line 188). Verified against results/paper_final/e5/plv.json: minimum alpha/beta/gamma PLV across all 5 arms x 2 splits is eegdfus/strict_test gamma = 0.98935, so ">= 0.98" is exactly true without rounding. No other changes; script rerun in the icml env, exits 0, in-script asserts (npz recomputation reproduces banked EEGDfus CCs 0.9805/0.8891) still pass; PNG visually confirmed — corrected annotation renders in Panel C top-right, all panels intact (A/B dumbbells with identity 0.71/0.70 recomputed in-script per the mandatory fix, C delta/theta bars, D leak strips n=1089/245/1334, E strict-segment overlay). No panels dropped; no new data surprises.

## fig-dsddpm — DS-DDPM reproduction findings  [appendix]
- **文件**: `artifacts/figures/v3/fig-dsddpm.pdf` ｜ **脚本**: `scripts/paper_final/figures/fig_dsddpm.py`
- **支撑论断**: Under twelve frozen protocol variants the DS-DDPM cross-subject backbone sits at chance (0.25-0.28 vs published 50.6/52.9%) while EEGNet reaches 66% on the same data, and its generated subject noise shows no subject specificity (matched 0.392 vs mismatched 0.393) — reported factually as a reproduction finding.
- **诚实性要点**: Caveats stated in caption: the release has no denoise-recorded-EEG path (both samplers start from Gaussian noise), the classifier is unreleased, the Table II computation is unreleased, and the PSD-signature choice was frozen before numbers were seen. This is a reproduction report, not an attack; the converged-training panel exists precisely to rule out the cheap explanation.
- **渲染记录/取舍**: All three panels built; none dropped. Mandatory fix applied: the EEGNet within-participant reference is read at runtime from results/paper_final/bci2a/route1.json accuracy_means.RAW (0.5926) and labeled "EEGNet-8,2 within 59.3%"; 66% appears nowhere. Verdict.fix items applied: (1) headerless train_metrics.csv column mapping hardcoded as [total, base, time_diff, noise_kl, sub_arc, orth] with a comment citing the writerow in scripts/paper_final/dsddpm_repro.py; (2) Panel B prints the honest overall means computed from the matrices — real_real "diag 1.00 | off 0.89" (not 0.94) with subject 6 flagged by S6 ticks on both axes (its light cross is clearly visible), gen_real "diag 0.39 | off 0.39" on the same 0-1 color scale. Panel A shows all 12 variants as grouped rows (arm colors figstyle.C RAW/ICA/DSDDPM) with 9 per-training-subject column-mean dots plus the grand-mean marker per row, chance 0.25 (read from route1.json), published ICA 50.58%/DS-DDPM 52.87% (read from table1 json published_reference), plus the 9x9 accuracy-matrix trio (raw/ica/dsddpm_paper, shared scale, diagonal outlined). Data surprises worth knowing for the caption: (a) the time_diff component RISES during training (0.62 -> 1.40 epoch-mean) and noise_kl creeps up to ~9e-3 while total falls 3.38 -> 0.69 and sub_arc collapses to ~2e-4 (final-epoch mean; final-step ~1.2e-4) — convergence of total/sub_arc still rules out under-training, but the rising time_diff is visible in Panel C and should be acknowledged; (b) faint per-step traces behind the epoch-mean curves in Panel C are raw data, not uncertainty bands (house rule 2 respected; no CIs/error bars anywhere). Loss-component colors are local non-arm colors (Tol-style) to avoid colliding with the paper-wide arm palette; total uses the neutral reference dark.

## fig-bench-snr — Single-channel benchmark across SNR  [appendix]
- **文件**: `artifacts/figures/v3/fig-bench-snr.pdf` ｜ **脚本**: `scripts/paper_final/figures/fig_bench_snr.py`
- **支撑论断**: Our noisy-conditioned diffusion is competitive on the EEGdenoiseNet grid (beats the unconditional prior and 2/5 supervised baselines) but does not beat the best supervised CNNs — context, not a contest.
- **诚实性要点**: SDEdit underperforms even the Noisy input on EOG (CC 0.6083 vs 0.6691) and CondDiff loses to SimpleCNN/NovelCNN — all eight lines plotted, none dropped, per the no-overclaiming discipline.
- **渲染记录/取舍**: 2x3 grid rendered (rows EOG/EMG; columns CC linear, RRMSE_t and RRMSE_s on log axes — Noisy EOG RRMSE_s spans 0.4-21.3, so log scale was required to keep the 6 denoiser arms readable). All 8 arms x 11 SNR levels plotted from the two CSVs; no panel dropped. Overall (SNR-pooled) rows are excluded from the lines and shown instead as a marginal "all" dot strip right of each panel; CC panels annotate only the two decision-relevant overall values read at runtime (EOG: CondDiff 0.888 vs SimpleCNN 0.934; EMG: CondDiff 0.762 vs NovelCNN 0.877). Direct color-matched arm labels (isotonic-dodged, no legend box) sit in the rightmost column per row. verdict.fix applied as runtime asserts: EOG CondDiff CC 0.8880 beats exactly 2/5 supervised arms (FCNN 0.8771, RNN_LSTM 0.8238), EMG CondDiff 0.7625 beats 0/5, CondDiff beats SDEdit and Noisy on both artifacts, and SDEdit EOG 0.6083 < Noisy 0.6691 (visible as the olive line under the gray dashed line in panel A) — script fails if the CSVs ever disagree with these caption claims. No data surprises: both CSVs were complete and matched every quoted number exactly. No CIs/error bars anywhere; all colors from figstyle.C.

## fig-width-locality — Interval width localization  [optional]
- **文件**: `artifacts/figures/v3/fig-width-locality.pdf` ｜ **脚本**: `scripts/paper_final/figures/fig_width_locality.py`
- **支撑论断**: Interval width localizes to unreliable calibrations: the operator-propagation term contributes a roughly ~0.30 mean share of squared width and grows with calibration within-variance, and gated cells sit where the shrinkage/reliability machinery says they should.
- **诚实性要点**: Supports the mechanism narrative only; the dev temperature ranges (2.30-2.55 vs 2.90-3.20) quoted alongside it in the text have no banked sweep data and stay text-only.
- **渲染记录/取舍**: Data surprise: the spec's claim that the operator width share "grows with calibration within-variance" is NOT in the banked data — Spearman(share, log within_v) = 0.014 (p=0.81) and decile-binned medians are flat at ~0.30 across the whole 7.6e-5..64 range. The figure shows this honestly via a binned-median line; the paper text for Sec 5.4 should soften that clause. What the data DOES support and the figure shows: (1) mean operator share 0.30 (runtime-computed dashed reference, marginal histogram); (2) EB shrinkage lambda declines with within-variance (Spearman -0.52), visible as the viridis color gradient left-to-right; (3) all 12 hard-gated cells (4.4%) have lam=0 and sit at within_v >= 0.44 (the top of the range) — gates localize exactly where the reliability machinery says. No panels dropped; spec's verdict.fix was empty. Colors: viridis for the continuous lam covariate and neutral grays (no experiment arms appear, so figstyle.C arm colors are not implicated); no error bars or CIs anywhere. Script exits 0; PDF+PNG verified on disk.

## 做不出的 9 个缺口
见 `FIGURE_PLAN_V3.md` 末节（需小规模重导出或保留正文文字）。
