# PhysioMotion subject-aware masked restoration

Routing decision: `PHYSIOMOTION_SUBJECT_RETRIEVAL_HEADROOM_NO_GO`.

The preregistered no-training retrieval headroom gate failed. Therefore the retrieval-conditioned deterministic and masked clean diffusion models were not trained, no one-seed model screen was run, no extra seeds were submitted, and sealed participants remained unopened.

This fail-closed result constrains the fixed K=8, two-second, z-normalized-correlation clean-patch retrieval representation on repeated-run development data. It is not a negative claim about masked diffusion or personalization families.
