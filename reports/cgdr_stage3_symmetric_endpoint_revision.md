# Klados stage-3 symmetric endpoint revision

The v3 attempt, including Slurm job 919843, is invalid for matched incremental-
value evidence because deterministic checkpoint selection used development
clean-target loss. It remains preserved under its original result root.

The replacement deterministic v4 and conditional v2 protocols both use an
operator-scope-isolated, fixed 6000-optimizer-update endpoint. Development loss
is diagnostic only and cannot choose a checkpoint or update count. Comparisons
remain exploratory source-record diagnostics, not formal G1/G3 evidence.
