# V30 common panel

The outcome-blind manifest contains 720 fixed corrected-coordinate rows. Each participant/session/task contributes the same registered count. Paired and natural inference use identical panel indices across candidates and seeds. Natural auxiliary arrays were opened only after the output freeze.
