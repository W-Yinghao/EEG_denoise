# V41R natural results

Not run. The paired POP route failed the preregistered population-validity diagnosis, so frozen
natural inference and the post-freeze EOG evaluator were not launched. Query-EOG inference reads,
evaluator reads, and sealed reads are all zero. No MATCH−POP natural contrast is interpreted.
