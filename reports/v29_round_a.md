# V29 Round A

The validation-only selection retained the `rank` adapter (`lambda_ctx=0.1`). Selection jointly considered absolute paired fidelity, MATCH−WRONG, MATCH−PopAdapter, corrected natural artifact/retention, increment size, and stability; no test participants or automatic scientific threshold were used.
