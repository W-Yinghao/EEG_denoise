# Leakage tests

This namespace will enforce participant, session, support/query and visible-field isolation.
Scientific leakage tests are pending frozen data/split schemas and must run through Slurm.
