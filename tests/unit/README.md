# Contract unit tests

New CSPD unit tests belong here and must be submitted through Slurm. Mathematical endpoint and
`NULL` tests remain blocked until `CONFLICT-SCI-001` is resolved; legacy top-level tests are not
evidence for the current protocol.
